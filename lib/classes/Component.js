export class Component {
    regex;
    client;
    constructor(client, regex) {
        this.regex = regex;
        this.client = client;
    }
    button(interaction) {
        interaction.customId;
    }
}
